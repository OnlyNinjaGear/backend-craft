{
  "name": "ts-fastify-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Intentionally flawed Fastify fixture for forward-testing backend-craft and Semgrep rules.",
  "scripts": {
    "start": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
